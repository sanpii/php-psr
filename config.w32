// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("psr", "for psr support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("psr", "enable psr support", "no");

if (PHP_PSR != "no") {
	EXTENSION("psr", "psr.c");
}

